spring:
  application:
    name: Login-Server-Session
 ## redis 配置
  redis:
    database: 0
    host: 112.74.43.136
    port: 6379
    password: jingbaobao

server:
  port: 8502




## eureka 客户端基本配置
eureka:
  instance:
    prefer-ip-address: true
    instance-id: ${spring.cloud.client.ip-address}:${server.port}
  client:
    serviceUrl:
      defaultZone: http://admin:pass@localhost:8100/eureka







basic:
  # cookie配置
  cookie:
    maxAge: 432000

