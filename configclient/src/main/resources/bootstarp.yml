spring:
  cloud:
    config:
      name: config-client
      #分支名称
      label: master
      profile:
      discovery:
        enabled: true
        service-id: Config-Server


#eureka:
#  client:
#    serviceUrl:
#      defaultZone: http://localhost:8100/eureka/